# Conditionals - M2.1 Core Layer

# if with else
if x > 0 do
  "positive"
else
  "non-positive"
end

# if without else
if condition do
  action()
end

# unless
unless error do
  proceed()
end

# case expression
case value do
  1 -> "one"
  2 -> "two"
  _ -> "other"
end
